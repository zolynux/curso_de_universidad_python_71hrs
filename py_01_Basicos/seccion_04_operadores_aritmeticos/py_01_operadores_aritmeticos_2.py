# Se definen dos variables con valores numéricos
operadorA = 3
operadorB = 2

# Se realiza la suma de las variables y se guarda el resultado en una nueva variable
suma = operadorA + operadorB

# Se imprime un mensaje con los valores de las variables y el resultado de la suma
print(f"El primer número de A: {operadorA} y el segundo número de B: {operadorB}")
print(f"La suma: {suma}")

# Se realiza la resta de las variables y se guarda el resultado en una nueva variable
resta = operadorA - operadorB

# Se imprime el resultado de la resta
print(f"Resultado resta: {resta}")

# Se realiza la multiplicación de las variables y se guarda el resultado en una nueva variable
multiplicacion = operadorA * operadorB

# Se imprime el resultado de la multiplicación
print(f"Resultado multiplicación: {multiplicacion}")

# Se realiza la división de las variables y se guarda el resultado en una nueva variable
division = operadorA / operadorB

# Se imprime el resultado de la división
print(f"Resltudao división: {division}")

# Se realiza la división entera de las variables y se guarda el resultado en una nueva variable
division2 = operadorA // operadorB

# Se imprime el resultado de la división entera
print(f"Reslutado división (//): {division2}")

# Se realiza la potenciación de las variables y se guarda el resultado en una nueva variable
exponente = operadorA**operadorB

# Se imprime el resultado de la potenciación
print(f"Resultado exponente: {exponente}")

# Se realiza el módulo de las variables y se guarda el resultado en una nueva variable
modulo = operadorA % operadorB

# Se imprime el resultado del módulo
print(f"Resultdo residuo división (módulo): {modulo}")
