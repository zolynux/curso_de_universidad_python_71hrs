# Se asignan valores a las variables "a" y "b"
a = 4
b = 2

# Se imprime una cadena formateada con los valores de "a" y "b"
print(f"el numero de a es {a} y el numero de b es {b}")

# Se compara "a" y "b" con el operador "==" y se guarda el resultado en "resultado"
resultado = a == b

# Se imprime una cadena formateada con el resultado de la comparación "a == b"
print(f"Resultado a == b: {resultado}")

# Se compara "a" y "b" con el operador "!=" y se guarda el resultado en "resultado"
resultado = a != b

# Se imprime una cadena formateada con el resultado de la comparación "a != b"
print(f"Resultado a != b: {resultado}")

# Se compara "a" y "b" con el operador ">" y se guarda el resultado en "resultado"
resultado = a > b

# Se imprime una cadena formateada con el resultado de la comparación "a > b"
print(f"Resultado a > b: {resultado}")

# Se compara "a" y "b" con el operador ">=" y se guarda el resultado en "resultado"
resultado = a >= b

# Se imprime una cadena formateada con el resultado de la comparación "a >= b"
print(f"Resultado a >= b: {resultado}")

# Se compara "a" y "b" con el operador "<" y se guarda el resultado en "resultado"
resultado = a < b

# Se imprime una cadena formateada con el resultado de la comparación "a < b"
print(f"Resultado a < b: {resultado}")

# Se compara "a" y "b" con el operador "<=" y se guarda el resultado en "resultado"
resultado = a <= b

# Se imprime una cadena formateada con el resultado de la comparación "a <= b"
print(f"Resultado a <= b: {resultado}")
